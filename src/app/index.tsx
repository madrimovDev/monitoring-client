import React from 'react'
import { ChakraProvider } from '@chakra-ui/react'
import { theme } from '@/shared'

const App = () => {
	return <ChakraProvider theme={theme}>asd</ChakraProvider>
}

export default App
